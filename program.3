#include <stdio.h>

int main() {
    char a[100], b[100];
    int i = 0;

    scanf("%s%s", a, b);

    while (a[i] == b[i] && a[i] != '\0')
        i++;

    if (a[i] == b[i])
        printf("Equal");
    else
        printf("Not Equal");

    return 0;
}
