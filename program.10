#include <stdio.h>

int main() {
    char str[100];

    fgets(str,100,stdin);

    int i=0,count=0;

    while(str[i]){
        if(str[i]==' ')
            count++;
        i++;
    }

    printf("%d",count);
}

