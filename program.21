#include <stdio.h>

int main() {
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str);

    if (str[0] >= 'a' && str[0] <= 'z')
        str[0] = str[0] - 32;

    printf("Result: %s", str);

    return 0;
}
