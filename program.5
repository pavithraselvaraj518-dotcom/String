#include <stdio.h>

int main() {
    char str[100];
    int i = 0;

    scanf("%s", str);

    while (str[i]) {
        if (str[i] >= 'a' && str[i] <= 'z')
            str[i] -= 32;
        i++;
    }
